package org.example.Ex03_Selenium_Commands;

public class MouseKeyboardActions {
    public static void main(String[] args) {
        // *** Mouse and Keyboard Actions ***

        /*
        1. Moves the mouse to a specific element.
        Actions actions = new Actions(driver);
        actions.moveToElement(element).perform(); // Example: Hovering over a menu.

        2. Performs a right-click (context click).
        actions.contextClick(element).perform(); // Useful for context menus.

        3. Drags an element and drops it onto another element.
        actions.dragAndDrop(sourceElement, targetElement).perform(); // Example: Reordering elements.

         */
    }
}
