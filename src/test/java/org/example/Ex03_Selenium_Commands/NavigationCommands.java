package org.example.Ex03_Selenium_Commands;

public class NavigationCommands {
    /*
     1. Navigates to a specific URL, similar to driver.get().
     driver.navigate().to("https://www.example.com");

     2. Simulates the browser's back button.
     driver.navigate().back();

     3. Simulates the browser's forward button.
     driver.navigate().forward();

     4. Refreshes the current page.
     driver.navigate().refresh(); // Useful when the page content changes dynamically.
    */
}
