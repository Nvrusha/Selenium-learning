package org.example.Ex03_Selenium_Commands;

public class ElementInteractionCommands {
    public static void main(String[] args) {
        // *** Element Interaction Commands ***

        /*
        1. Locates a single web element on the page.
        WebElement element = driver.findElement(By.id("username")); // Locator by ID.

        2. Locates multiple elements and returns a list.
        List<WebElement> elements = driver.findElements(By.className("item")); // Locator by class.

        3. Clicks on a web element (e.g., button, link).
        element.click(); // Common for buttons, links, or any clickable element.

        4. Enters text into an input field.
        element.sendKeys("test input"); // Example: Filling a text box.

        5. Retrieves the visible text of an element.
        String text = element.getText(); // Example: For getting paragraph or heading content.

        6. Clears the content of an input field.
        element.clear(); // Useful for editing pre-filled text fields.

         */
    }
}
