package org.example.Ex03_Selenium_Commands;

public class AdvancedCommands {
    public static void main(String[] args) {
        // *** Advanced Commands ***

        /*
        1. Switches the context to a frame (iframe).
        driver.switchTo().frame("frameName"); // Use frame name or index.

        2. Handles alerts (pop-ups).
        Alert alert = driver.switchTo().alert(); // Switch to alert.
        alert.accept(); // Clicks 'OK' on the alert.

        3. Retrieves all open browser window handles.
        Set<String> handles = driver.getWindowHandles(); // Useful for multi-tab or pop-up handling.

        4. Retrieves the current browser window handle.
        String currentHandle = driver.getWindowHandle(); // Identifies the active window.

         */
    }
}
